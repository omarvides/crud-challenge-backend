const { connect } = require('mongoose')
const modelSchema = require('../model-schema')

const Email = modelSchema.Email

function createEmail(params) {}

function getEmail(params) {}

function updateEmail(params) {}

function deleteEmail(params) {}

module.exports = {
  createEmail,
  getEmail,
  updateEmail,
  deleteEmail,
}
